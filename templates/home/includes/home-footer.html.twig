<div class="ui inverted vertical footer segment">
    <div class="ui container">
        <div class="ui stackable inverted divided equal height stackable grid">
            <div class="three wide column">
                <h4 class="ui inverted header">À propos</h4>
                <div class="ui inverted link list">
                    <a class="item" href="{{ path('mentions_legales') }}">Mentions légales</a>
                    <a class="item" href="{{ path('sitemap') }}">Sitemap</a>
                </div>
            </div>
            <div class="three wide column">
                <h4 class="ui inverted header">Services</h4>
                <div class="ui inverted link list">
                    <a class="item" href="{{ path('register') }}">Inscription</a>
                    <a class="item" href="{{ path('login') }}">Connexion Parent</a>
                    <a class="item" href="{{ path('ouvrir-journal') }}">Connexion Enfant</a>
                </div>
            </div>

        </div>
    </div>
</div>
